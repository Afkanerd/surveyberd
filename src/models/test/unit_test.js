const assert = require("chai").assert;

it('connect to database', (done) => {
    // done();
})

describe("vailid input", () => {
    it("valid email", (done) => {
        // done()
    })

    it("valid password", (done) => {
        // done()
    })
})

describe("invailid input", () => {
    it("invalid email", (done) => {
        // done()
    })

    it("invalid password", (done) => {
        // done()
    })
})

describe("empty input", () => {
    it("no email", (done) => {
        // done()
    })

    it("no password", (done) => {
        // done()
    })
})

it("harsh password", (done) => {
    // done();
})

describe("successfully created", () => {
    it("return id", (done) => {
        // done()
    })

    it("return email used to create", (done) => {
        // done()
    })
})

it("fail to create", () => {
    // done();
})